-- runtime/counterfactual.lua
-- Counterfactual branching is fully implemented inline in runtime/eval.lua
-- (see eval_expr, COUNTERFACTUAL_EXPR handler, lines ~301-422).
-- This module is not loaded at runtime; it exists only as a historical placeholder.

return {}
